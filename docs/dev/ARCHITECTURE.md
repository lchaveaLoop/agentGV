# Architecture Design

AgentGV 架构设计文档，详细描述系统的整体架构、组件设计和数据流。

---

## 📋 目录

- [系统概述](#系统概述)
- [架构演进](#架构演进)
- [核心组件](#核心组件)
- [部门架构](#部门架构)
- [Skill 系统](#skill-系统)
- [数据流](#数据流)
- [技术决策](#技术决策)
- [扩展性设计](#扩展性设计)

---

## 🎯 系统概述

AgentGV 是一个多 Agent 协作系统，采用部门化架构模拟政府组织，实现任务的自主路由、分配和执行。

### 设计理念

1. **部门化协作**: 模拟政府部门的分工协作模式
2. **智能路由**: Router 自动分析任务并分发给合适的部门
3. **Skill 模板化**: 基于技能领域而非固定 Agent 定义
4. **动态模型分配**: 根据任务类型自动选择最优模型
5. **自主执行**: Agent 在达到目标前不寻求人工干预

### 系统目标

- 提供自然语言到任务执行的无缝转换
- 支持多领域专业任务处理
- 实现高效的多 Agent 协作
- 保持系统的可扩展性和可维护性

---

## 📊 架构演进

| 版本 | 架构 | Agent 数 | 特点 |
|------|------|---------|------|
| V1 | 7 Agent | 7 | 初始架构，固定 Agent |
| V2 | 4 Agent | 4 | 简化架构 |
| V3 | 4+N 模板 | 4+15 | 引入 Skill 模板 |
| V4 | 7 人团队 | 7 | 完整部门 |
| V4.1 | 4 部门 | 4 | 能力合并优化 |
| V5 | 4+Admin | 5 | 自主执行增强 |

### 当前架构 (V5.0)

```
┌─────────────────────────────────────────────────────────────┐
│                    AgentGV Router                           │
│              (智能路由协调中心)                              │
│                                                             │
│  • Skill 匹配    • 模型分配    • 部门协调    • 自主闭环     │
└─────────────────────────────────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         │                    │                    │
         ▼                    ▼                    ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   Planning      │ │   Operations    │ │    Quality      │
│   规划局        │ │   执行部        │ │    质检部       │
│                 │ │                 │ │                 │
│ • 架构设计      │ │ • 功能开发      │ │ • 代码审查      │
│ • 技术方案      │ │ • 代码实现      │ │ • 测试验证      │
│ • 调研分析      │ │ • 系统集成      │ │ • Bug 检测      │
│ • 技术选型      │ │ • 文档编写      │ │ • 质量评估      │
│                 │ │ • 创意写作      │ │                 │
│ [Simulation]    │ │ [Software]      │ │ [All Skills]    │
│ [Research]      │ │ [Hardware]      │ │                 │
│                 │ │ [Creative]      │ │                 │
└─────────────────┘ └─────────────────┘ └─────────────────┘
         │                    │
         └─────────┬──────────┘
                   │
                   ▼
         ┌─────────────────┐
         │ Administration  │
         │   行政部        │
         │                 │
         │ • 任务协调      │
         │ • 自主执行      │
         │ • 多步骤工作流  │
         │ • 进度跟踪      │
         └─────────────────┘
```

---

## 🧩 核心组件

### 1. Router (路由器)

**角色**: 主 Agent，系统的入口和协调中心

**职责**:
- 接收用户输入
- 运行 Skill 匹配
- 选择目标部门
- 分配模型资源
- 协调多部门协作
- 自主闭环执行

**工作流程**:
```
用户请求 → 模型同步 → Skill 匹配 → 部门选择 → 任务委托 → 等待结果 → 返回用户
```

**关键文件**:
- `.opencode/agents/agentgv-router.md` - Agent 定义
- `.opencode/skill-matcher.js` - Skill 匹配器
- `.opencode/auto-sync-model.js` - 模型同步

### 2. Planning (规划局)

**角色**: 子 Agent，负责规划和设计

**职责**:
- 系统架构设计
- 技术方案制定
- 调研分析
- 技术选型
- 仿真建模

**Skills**:
- Simulation: matlab, fea, cfd
- Research: academic, market, data, product, competitor, ux

**关键文件**:
- `.opencode/agents/agentgv-planning.md`
- `.opencode/skills/simulation/`
- `.opencode/skills/research/`

### 3. Operations (执行部)

**角色**: 子 Agent，负责实现和开发

**职责**:
- 功能开发
- 代码实现
- 系统集成
- 文档编写
- 创意写作

**Skills**:
- Software: cpp, python, web, mobile, java, go, rust, devops
- Hardware: pcb, fpga, embedded
- Creative: fiction, technical, content, script, translation

**关键文件**:
- `.opencode/agents/agentgv-operations.md`
- `.opencode/skills/software/`
- `.opencode/skills/hardware/`
- `.opencode/skills/creative/`

### 4. Quality (质检部)

**角色**: 子 Agent，负责质量保障

**职责**:
- 代码审查
- 测试验证
- Bug 检测
- 质量评估

**Skills**:
- 支持所有 Skill 领域的审查

**关键文件**:
- `.opencode/agents/agentgv-quality.md`

### 5. Administration (行政部)

**角色**: 自主执行 Agent，负责协调和追踪

**职责**:
- 任务协调
- 自主执行
- 多步骤工作流
- 进度跟踪

**特性**:
- 自主决策，无需用户确认
- 可调用其他 Agent
- 错误自动恢复

**关键文件**:
- `.opencode/agents/agentgv-administration.md`

---

## 🏢 部门架构

### 能力映射

| 原部门 | 现归属 | 转移能力 |
|--------|--------|----------|
| Intelligence | Planning | 调研、分析、市场研究 |
| Communications | Operations | 文档编写、技术写作 |
| Administration | Router | 协调管理、任务分发 |

### 部门间通信

```
┌──────────────┐
│   Router     │
│  (Coordinator)│
└──────────────┘
       │
    ┌──┼──┐
    │  │  │
    ▼  ▼  ▼
┌────────┐ ┌────────────┐ ┌────────┐
│Planning│ │ Operations │ │Quality │
└────────┘ └────────────┘ └────────┘
       │         │              │
       └────┬────┘              │
            │                   │
            ▼                   ▼
       ┌────────────┐    ┌──────────┐
       │Administration│  │  Report  │
       └────────────┘    └──────────┘
```

---

## 🎯 Skill 系统

### Skill 分类

```
Skill System (5 Categories, 28 Skills)
│
├── Software (8 skills)
│   ├── cpp, python, web, mobile
│   └── java, go, rust, devops
│
├── Hardware (3 skills)
│   ├── pcb, fpga, embedded
│
├── Simulation (3 skills)
│   ├── matlab, fea, cfd
│
├── Creative (5 skills)
│   ├── fiction, technical, content
│   └── script, translation
│
└── Research (6 skills)
    ├── academic, market, data
    └── product, competitor, ux
```

### Skill 匹配流程

```
用户输入
    ↓
1. 预处理 (分词、标准化)
    ↓
2. 关键词匹配 (TF-IDF 加权)
    ↓
3. 类别识别 (基于关键词)
    ↓
4. 模型选择 (基于类别)
    ↓
5. 返回匹配结果
```

### Skill 配置示例

```json
{
  "skill_id": "cpp",
  "skill_name": "C++ Development",
  "category": "software",
  "keywords": ["C++", "cpp", "qt", "stl", "cmake"],
  "model": "qwen3-coder-plus",
  "temperature": 0.3,
  "responsible_agent": "operations"
}
```

---

## 🔄 数据流

### 单次请求流程

```
┌─────────┐
│  User   │
└────┬────┘
     │ 1. 发送请求
     ▼
┌─────────────────┐
│     Router      │
│  - 模型同步      │
│  - Skill 匹配    │
│  - 部门选择      │
└────┬────────────┘
     │ 2. 委托任务
     ▼
┌─────────────────┐
│  Target Agent   │
│  (Planning/     │
│  Operations/    │
│  Quality)       │
└────┬────────────┘
     │ 3. 执行任务
     ▼
┌─────────────────┐
│     Result      │
└────┬────────────┘
     │ 4. 返回结果
     ▼
┌─────────┐
│  User   │
└─────────┘
```

### 多部门协作流程

```
┌─────────┐
│  User   │
└────┬────┘
     │
     ▼
┌─────────────────┐
│     Router      │
│  (协调中心)      │
└────┬────────────┘
     │
     ├──────────────────┬──────────────────┐
     │                  │                  │
     ▼                  ▼                  ▼
┌─────────┐      ┌─────────────┐    ┌─────────┐
│Planning │      │ Operations  │    │ Quality │
│ 架构设计 │ ──→ │  功能开发   │ ──→│ 测试验证│
└─────────┘      └─────────────┘    └────┬────┘
                                          │
                                          ▼
                                    ┌─────────┐
                                    │  User   │
                                    └─────────┘
```

---

## 🔧 技术决策

### 1. 为什么使用部门化架构？

**决策**: 采用 4-5 部门架构而非固定 Agent

**原因**:
- ✅ 更符合人类组织的协作模式
- ✅ 便于扩展新 Skill
- ✅ 职责清晰，易于维护
- ✅ 支持多 Agent 协作

**权衡**:
- ⚠️ 路由逻辑稍复杂
- ⚠️ 需要明确的部门边界定义

### 2. 为什么使用 Skill 模板？

**决策**: 基于 Skill 而非固定 Agent

**原因**:
- ✅ 高度可扩展
- ✅ 支持多领域
- ✅ 模型配置灵活
- ✅ 易于添加新能力

### 3. 模型选择策略

**决策**: 动态模型分配

**规则**:
- 架构设计 → qwen3-max
- 复杂编码 → qwen3.5-plus
- 常规编码 → qwen3-coder-plus
- 简单任务 → qwen3-coder-next

**原因**:
- ✅ 优化成本
- ✅ 保证质量
- ✅ 灵活适配

### 4. 自主执行原则

**决策**: Agent 自主执行，无需频繁确认

**原因**:
- ✅ 提高效率
- ✅ 减少用户负担
- ✅ 更接近真实协作

**安全保障**:
- ⚠️ 重大决策需用户确认
- ⚠️ 资源消耗需告知
- ⚠️ 不可恢复错误需报告

---

## 🔌 扩展性设计

### 添加新 Skill

1. 在 `.opencode/config/skills.json` 添加配置
2. 创建 Skill 模板 `.opencode/skills/<category>/<skill>/SKILL.md`
3. 更新关键词映射
4. 运行测试验证

### 添加新部门

1. 创建 Agent 定义 `.opencode/agents/agentgv-<name>.md`
2. 配置模型和职责
3. 更新 Router 路由逻辑
4. 添加测试用例

### 修改模型配置

1. 编辑 `.opencode/config/models.json`
2. 运行验证器 `node .opencode/scripts/validators/config-validator.js`
3. 运行测试确保兼容

### 扩展点

| 扩展点 | 位置 | 难度 |
|--------|------|------|
| 新 Skill | `skills.json` + 模板 | 低 |
| 新模型 | `models.json` | 低 |
| 新部门 | `agents/` + Router | 中 |
| 新路由规则 | `skill-matcher.js` | 中 |
| 新任务类型 | Router 定义 | 低 |

---

## 📐 设计原则

### 1. 单一职责

每个部门和 Agent 有明确的职责范围。

### 2. 松耦合

部门间通过 Router 协调，减少直接依赖。

### 3. 高内聚

相关功能集中在同一部门。

### 4. 可扩展

通过 Skill 模板轻松添加新能力。

### 5. 可测试

每个组件可独立测试。

### 6. 文档驱动

所有组件有完整的文档说明。

---

## 🔮 未来规划

### 短期 (V5.x)

- [ ] 增强 Skill 匹配算法
- [ ] 添加更多 Skill 模板
- [ ] 改进多部门协作

### 中期 (V6.x)

- [ ] 支持自定义 Agent
- [ ] Web 监控界面
- [ ] 任务执行历史记录

### 长期 (V7.x)

- [ ] 机器学习优化路由
- [ ] 分布式执行
- [ ] 插件系统

---

**最后更新**: 2026-02-25  
**版本**: V5.0.0  
**状态**: 当前架构稳定，持续优化中
